# 1559. Detect Cycles in 2D Grid

[Problem Link](https://leetcode.com/problems/detect-cycles-in-2d-grid/description/)

### Review

I missed the `&` in the `visited` parameter, so I encountered a TLE error.
Also, the starting point isn't always `(0,0)`, which makes the problem harder to solve.